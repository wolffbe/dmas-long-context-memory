fastapi>=0.116.2,<1.0.0
uvicorn>=0.34.0,<1.0.0
pydantic>=2.12.3
openai>=2.6.1
httpx>=0.27.0
mem0ai==2.0.1
graphiti-core==0.29.0
cognee==0.5.6
cognee-community-vector-adapter-qdrant==0.2.4
# cognee 0.5.6 lazily imports `langfuse` from its observability layer.
# Without it the import raises ModuleNotFoundError mid-pipeline and the
# error bubbles up into the benchmark CSV. We don't use langfuse's SDK
# directly (we ship traces via OTel/OTLP), but cognee needs the module
# present at import time, so pin a compatible release.
langfuse>=2.60.0,<3.0.0
qdrant-client>=1.10.0
python-dotenv==1.0.1
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-httpx>=0.48b0
opentelemetry-instrumentation-requests>=0.48b0